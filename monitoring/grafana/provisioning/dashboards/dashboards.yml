apiVersion: 1

providers:
  - name: 'LumenQuery Dashboards'
    orgId: 1
    folder: 'LumenQuery'
    folderUid: 'lumenquery'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
